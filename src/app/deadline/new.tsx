import React from 'react';
import DeadlineFormContainer from '@/components/forms/DeadlineFormContainer';

const NewDeadline = () => {
  return <DeadlineFormContainer mode="new" />;
};

export default NewDeadline;
